name: Yumeka Animations Studio Engine - Build APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Tipo de build'
        required: true
        default: 'debug'
        type: choice
        options:
          - debug
          - release

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Instalar Gradle 8.7
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.7'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.kts', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Permissao gradlew
        run: chmod +x gradlew

      - name: Aceitar licencas SDK
        run: yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses || true

      - name: Build APK Debug
        if: github.event_name != 'workflow_dispatch' || github.event.inputs.release_type == 'debug'
        run: |
          gradle :app:assembleDebug --stacktrace 2>&1 | tee build-output.txt
          exit ${PIPESTATUS[0]}

      - name: Build APK Release
        if: github.event.inputs.release_type == 'release'
        run: |
          gradle :app:assembleRelease --stacktrace 2>&1 | tee build-output.txt
          exit ${PIPESTATUS[0]}

      - name: Upload log de build (em caso de erro)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ github.sha }}
          path: build-output.txt
          retention-days: 7

      - name: Mostrar erros Kotlin no log
        if: failure()
        run: grep -E "error:|warning:|Exception|FAILED" build-output.txt | head -100 || true

      - name: Listar APKs gerados
        run: find . -name "*.apk" 2>/dev/null || echo "Nenhum APK encontrado"

      - name: Upload APK Debug
        if: success() && (github.event_name != 'workflow_dispatch' || github.event.inputs.release_type == 'debug')
        uses: actions/upload-artifact@v4
        with:
          name: yumeka-animations-debug-${{ github.sha }}
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 30

      - name: Upload APK Release
        if: success() && github.event.inputs.release_type == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: yumeka-animations-release-${{ github.sha }}
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 90
