<?xml version="1.0" encoding="utf-8"?>
<!--
    Card de projeto — estilo Godot com borda esquerda colorida + glassmorphism
    Borda esquerda: gradiente vermelho→roxo (accent)
    Fundo: dark glass com borda sutil
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/item_project_bg"
    android:layout_marginBottom="8dp"
    android:gravity="center_vertical"
    android:clipChildren="false">

    <!-- Barra vertical esquerda com gradiente (marca visual estilo Godot) -->
    <View
        android:layout_width="4dp"
        android:layout_height="52dp"
        android:background="@drawable/item_project_accent"
        android:layout_marginStart="0dp"
        android:layout_marginEnd="12dp" />

    <!-- Ícone do projeto -->
    <TextView
        android:id="@+id/project_icon"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:text="🎬"
        android:textSize="20sp"
        android:gravity="center"
        android:background="@drawable/icon_project_bg"
        android:layout_marginEnd="12dp" />

    <!-- Informações -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <!-- Nome do projeto -->
        <TextView
            android:id="@+id/project_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#EEEEFF"
            android:textSize="14sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Data + versão numa linha -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="3dp">

            <TextView
                android:id="@+id/project_date"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="#50507A"
                android:textSize="11sp" />

            <!-- Badge de versão pill -->
            <TextView
                android:id="@+id/project_version"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#9B6DFF"
                android:textSize="9sp"
                android:textStyle="bold"
                android:background="@drawable/badge_background"
                android:paddingStart="7dp"
                android:paddingEnd="7dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:layout_marginEnd="4dp" />

        </LinearLayout>

        <!-- Path (muito discreto) -->
        <TextView
            android:id="@+id/project_path"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="#30305A"
            android:textSize="9sp"
            android:maxLines="1"
            android:ellipsize="start" />

        <!-- Mensagem de erro -->
        <TextView
            android:id="@+id/project_error_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:textColor="#FF4455"
            android:textSize="10sp"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Favorito -->
    <TextView
        android:id="@+id/project_favorite"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="★"
        android:textColor="#FBBF24"
        android:textSize="13sp"
        android:layout_marginEnd="6dp"
        android:visibility="gone" />

    <!-- Seta de navegação -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="›"
        android:textColor="#2A2A50"
        android:textSize="24sp"
        android:layout_marginEnd="14dp" />

</LinearLayout>
